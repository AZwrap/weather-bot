httpx[http2]>=0.27  # http2 extra pulls in `h2` for persistent HTTP/2 connections (eliminates ~54ms TLS handshake on every request)
numpy>=2.0
pandas>=2.2
scipy>=1.13
python-dateutil>=2.9
openpyxl>=3.1

# Dashboard (optional but installed by default for ease of deploy)
streamlit>=1.30
plotly>=5.18
